"use strict";
//import RelayTransaction from "../types/RelayTransaction.interface";
/*import axios from "axios";
import { ContractReceipt } from "ethers";
import { Task } from "../types";
import { OPS_TASKS_API } from "../constants";

// const createTask()
// renameTask()
// pauseTask()
// cancelTask()
// getTaskStatus() ?

export async function createTask(): Promise<ContractReceipt> {
  return null;
}

export async function pauseTask(): Promise<ContractReceipt> {
  return null;
}

export async function cancelTask(): Promise<ContractReceipt> {
  return null;
}

export async function renameTask(
  chainId: number,
  taskId: string,
  name: string
): Promise<Task> {
  try {
    return await axios.post(`${OPS_TASKS_API}/tasks/${chainId}/${taskId}`, {
      name,
    });
  } catch (error) {
    console.error(error);
  }
}
*/
