import "ethers";
import { Signer } from "@ethersproject/abstract-signer";
import { Task, TaskReceipt } from "../types";
export declare class GelatoOpsSDK {
    private _chainId;
    private _signer;
    private _ops;
    constructor(chainId: number, signer: Signer);
    getActiveTasks(): Promise<Task[]>;
    createTask(executorAddress: string, executorSelector: string, resolverAddress: string, resolverData: string, name?: string): Promise<TaskReceipt>;
    cancelTask(taskId: string): Promise<TaskReceipt>;
    private _createTaskName;
    renameTask(taskId: string, name: string): Promise<void>;
    private _postTaskApi;
    private _logTaskApiError;
}
