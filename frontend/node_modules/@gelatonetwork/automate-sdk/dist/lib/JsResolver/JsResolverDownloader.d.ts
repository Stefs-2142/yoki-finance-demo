import { JsResolverSchema } from "../../types";
export declare class JsResolverDownloader {
    private readonly _userApi;
    constructor();
    fetchSchema(cid: string): Promise<JsResolverSchema>;
}
