"use strict";
var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.JsResolverStorage = void 0;
const axios_1 = __importDefault(require("axios"));
const constants_1 = require("../../constants");
const utils_1 = require("../../utils");
class JsResolverStorage {
    constructor(signer, signature) {
        this._signer = signer;
        this._userApi = axios_1.default.create({
            baseURL: constants_1.OPS_USER_API,
        });
        this._signature = signature;
    }
    get(chainId, taskId) {
        return __awaiter(this, void 0, void 0, function* () {
            try {
                const address = yield this._signer.getAddress();
                const authToken = yield this._signature.getAuthToken();
                const res = yield this._userApi.get(`/users/${address}/resolver-storage/${chainId}/${taskId}`, { headers: { Authorization: `Bearer ${authToken}` } });
                return res.data;
            }
            catch (err) {
                const errMsg = (0, utils_1.errorMessage)(err);
                throw new Error(`Fail to get storage for task "${taskId}". \n${errMsg}`);
            }
        });
    }
}
exports.JsResolverStorage = JsResolverStorage;
