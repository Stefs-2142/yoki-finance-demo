import { Web3FunctionSchema } from "../../types";
export declare class Web3FunctionDownloader {
    private readonly _userApi;
    constructor();
    fetchSchema(cid: string): Promise<Web3FunctionSchema>;
}
